function inscrever(){
    const width = 800;
    const heigth = 600;
    const left = (screen.width / 2) - (width / 2);
    const top = (screen.heigth / 2) - (heigth / 2);
    window.open('pagina4.html', 'pagamento', 'width=${width}, heigth=${heigth}, top=${top}, lefth=${left}');
}

function Reserva(){
    const width = 800;
    const heigth = 600;
    const left = (screen.width / 2) - (width / 2);
    const top = (screen.heigth / 2) - (heigth / 2);
    window.open('pagina5.html', 'pagamento', 'width=${width}, heigth=${heigth}, top=${top}, lefth=${left}');
}